# Elenco istruzioni visualizzate:

Per inizializzare una repository ho usato:
```bash
git init
```

Il secondo comando che abbiamo visto, per visualizzare lo status della repository è:
```bash
git status
```